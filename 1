if __name__ == "__main__":
    # 换成一个公开的测试接口（不需要密钥）
    url = "https://httpbin.org/post"
    
    headers = {
        "Content-Type": "application/json"
    }

    payload = {
        "test_message": "Hello, 测试API调用！"
    }

    print(" 正在调用测试 API...")
    result = call_api(url, method='POST', headers=headers, json_data=payload)

    if result:
        print(调用成功！服务器返回数据：")
        # httpbin.org 返回的 json 里会包含你发的 json
        print(json.dumps(result, indent=4, ensure_ascii=False)) 
    else:
        print("调用失败。")
